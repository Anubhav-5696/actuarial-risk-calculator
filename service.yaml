apiVersion: v1
kind: Service
metadata:
  name: actuarial-risk-calculator-service
spec:
  type: NodePort
  selector:
    app: actuarial-risk-calculator
  ports:
  - port: 5000
    targetPort: 5000
    nodePort: 30007